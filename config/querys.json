{
    
    "getApplicationAdmins": "SELECT file_name FROM test",
    "validateUser": "SELECT username FROM user WHERE username = $1",
    "validatePassword": "SELECT user_password FROM user WHERE user = $1",
    "registerUser": "INSERT INTO user (username, email, user_password, id_enterprises, id_user_type) VALUES ($1, $2, $3, $4, $5)"
}